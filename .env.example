# Server
PORT=3000

# Allowed CORS origins (comma-separated)
# Empty = all allowed (development only!)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Scraper
CHROME_PATH=/usr/bin/google-chrome-stable

# Cache
CACHE_TTL_SECONDS=30
DB_PATH=cache.db